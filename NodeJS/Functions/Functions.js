
function subtraction(q, w) {
    const result = q - w;
    console.log(result);
}

subtraction(20, 10);


function Addition(a, b) {

    function sum(x, y) {
        const result = x + y;
        return result;

    }
    const finalresult = sum(a, b)
    return finalresult;
}

const res = Addition(10, 20);
console.log(res);
